use iced::{widget::button, Background, Color, Theme};

//#[derive(Debug, Clone, Copy, Default)]
//pub struct Base16Theme;

//impl From<Base16Theme> for Theme {
    //fn from(_: Base16Theme) -> Self {
        //Theme::Custom(Box::new(Base16Theme))
    //}
//}

//impl button::Style for Base16Theme {
    //type Style = Theme;

    //fn active(&self, _theme: Theme) -> button::Style {
        //button::Style {
            //background: Some(Background::Color(Color::from_rgb(255, 255, 255))),
            //border_radius: 16.0,
            //..Default::default()
        //}
    //}
//}
